export PATH=$HOME/.emacs.d/bin:$PATH

# Open Emacs with current dir as a project
alias ecdp='touch ./.projectile && emacsclient -a emacs -t .projectile && rm ./.projectile'
# Open Emacs with current dir as a project
alias ef='emacsclient -a emacs -t'
